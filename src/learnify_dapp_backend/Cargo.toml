[package]
name = "learnify_dapp_backend"
version = "0.2.0"
edition = "2021"

[dependencies]
ic-cdk = "0.6.0"
ic-cdk-macros = "0.6.0"
serde = { version = "1.0", features = ["derive"] }
lazy_static = "1.4.0"

[lib]
crate-type = ["cdylib"]
